<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UCSC Music Map</title>
    <link rel="stylesheet" href="/css/booyah.css">
</head>
<body>
    

    
    <h1>Welcome to the UCSC Music Map! Please post a comment of where you are and what song you were listening to!</h1>
    

    <div id="img-box">
        <img src="/img/map_proto.jpeg" alt="Map1" class="responsiveImage">
    </div>

    <section class="highlight bordered">
        text text text
    </section>

    <textarea id="textInput" placeholder="Type your comment here..."></textarea>
    <button id="postTextButton">Post Comment</button>
    <button id="viewPostsButton">View Posts</button>

    <div id="postsContainer">
        <% if (comments.length === 0) { %>
            <p>No comments yet. Be the first to comment!</p>
        <% } else { %>
            <% comments.forEach((comment) => { %>
                <div class="post">
                    <p><%= comment.content %></p>
                    <small><%= new Date(comment.timestamp).toLocaleString() %></small>
                </div>
            <% }) %>
        <% } %>
    </div>
    
    <script src="/js/booyah.js"></script> <!-- Correct path -->
</body>
</html>
